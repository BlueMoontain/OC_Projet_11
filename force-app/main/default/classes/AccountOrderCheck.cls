public class AccountOrderCheck {
    public static void checkOrders(Id accountId) {
        // Récupére les Order associés au compte
        // Si aucun autre Order n'est associé, décochez la case Active__c
    }

    // méthode checkOrders invoquée dans un trigger "OrdersDeletionTrigger" qui se déclenche lorsqu'un Order est supprimé
}
