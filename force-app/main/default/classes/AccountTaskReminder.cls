public class AccountTaskReminder {
    public static void createReminder(Account acc) {
        // Crée une nouvelle tâche de rappel pour le compte
    }
    // méthode createReminder invoquée dans un batch "AccountTaskReminderBatch" programmé par scheduler pour le 1er lundi de chaque mois 
}
